import LoadingScreen from '@/components/LoadingScreen'

export default function Loading() {
  // Inside the dashboard shell — fills the content column area, not the
  // sidebar. Reuses the branded LoadingScreen.
  return <LoadingScreen />
}
